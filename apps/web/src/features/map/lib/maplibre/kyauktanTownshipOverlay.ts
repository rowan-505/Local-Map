/**
 * Kyauktan township map overlay (fill + outline) removed — bounds still use
 * Township GeoJSON remains available for bounds / future UX; initial map camera uses country bounds.
 */
import type { MapEngine } from '../mapEngineTypes';

export function ensureKyauktanTownshipOverlay(map: MapEngine): void {
  void map;
}
