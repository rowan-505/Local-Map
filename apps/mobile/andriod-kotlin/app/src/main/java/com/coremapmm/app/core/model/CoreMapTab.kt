package com.coremapmm.app.core.model

enum class CoreMapTab(
    val label: String,
) {
    Discover(label = "Discover"),
    Transit(label = "Transit"),
    MyMap(label = "My Map"),
    Settings(label = "Settings"),
}
