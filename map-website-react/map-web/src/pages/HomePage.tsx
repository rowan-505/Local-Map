import MapView from "../features/map/coponents/MapView";

export default function HomePage() {
    return (
        <main className="w-screen h-screen overflow-hidden">
            <MapView />
        </main>
        
    );
}