# Copy to imports/<name>.env and run:
#   DRY_RUN=true ./run_backfill_streets_admin_area_id_loop.sh imports/<name>.env
#   CONFIRM_WRITE=true ./run_backfill_streets_admin_area_id_loop.sh imports/<name>.env
#
# Prerequisite (once per database):
#   cd ../admin-hierarchy-repair
#   CONFIRM_WRITE=true ./run_admin_hierarchy_repair.sh --from-stage 03 imports/<name>.env

LOCAL_DATABASE_URL='postgresql://USER:PASSWORD@127.0.0.1:5432/coremap'
# DATABASE_URL='postgresql://...'

DRY_RUN=false
CONFIRM_WRITE=false
LIMIT_ROWS=5000
LAST_ID=0
FORCE_RECALCULATE_VERIFIED=false
FORCE_MANUAL_OVERRIDE=false
