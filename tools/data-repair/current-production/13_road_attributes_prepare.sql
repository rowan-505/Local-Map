-- Prompt 7 prepare note: safe_to_apply = non-manual, source tag clear, column differs.
-- Verified rows included for mechanical attribute sync (same rule as road class FK).
SELECT 'safe_attr_updates_estimate' AS note, 8836+22929+255 AS approx_oneway_bridge_tunnel;
